# WireGuard del probe agent

La guía y las plantillas canónicas para ambos hosts están en [fase 5](../../phase5/wireguard/README.md). La configuración de `deploy/phase4/.env` debe usar el dominio público resuelto por el túnel hacia `10.44.0.1`, no una IP HTTPS sin certificado válido.
