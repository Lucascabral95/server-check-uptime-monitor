# Use a dedicated key restricted to this bucket and prefix.
AWS_ENDPOINT_URL=https://s3.example.com
AWS_S3_BUCKET=server-check-backups
AWS_S3_PREFIX=server-check/
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=replace-with-write-only-access-key
AWS_SECRET_ACCESS_KEY=replace-with-secret-access-key
