receivers:
  otlp:
    protocols:
      http:
      grpc:
exporters:
  debug:
    verbosity: basic
processors:
  batch: {}
service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]
