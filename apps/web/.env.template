PORT=3000
NEXT_PUBLIC_API_URL_BACKEND=http://localhost:4000/api/v1

# Server-only: usada por app/api/backend/[...path]/route.ts (el proxy que
# reenvía requests al backend real con el JWT de la cookie httpOnly). Si no
# se define, cae a NEXT_PUBLIC_API_URL_BACKEND.
BACKEND_API_URL=http://localhost:4000/api/v1

NEXT_PUBLIC_URL_FRONTEND=http://localhost:3000

# AWS ENVS
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-xxxxxxx
NEXT_PUBLIC_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxx